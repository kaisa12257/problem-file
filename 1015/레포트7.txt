이번 7장 강의의 활용에 대하여 배웠음 들으면서 객체, 배열 math의 활용에 대하여 배웠으며
이때 math는 반올림 난수값등의 수학적 계산에 많이 사용되며 또한 문자의 대,소문자와 관령이 있는 string에 
대해서도 배웠다.그리고 자바 스크립트에서 프로퍼티 라는 것이 존재하는게 이것은 객체가 가지는 고유한 속성을 의미한다.
그리고 그 객체에서는 코어 객체, HTML DOM객체 브라우저 객체라는 것이 존재한다.
코어객체에는 ARRAY, DAte, String, Math가 있다. let today = new Date(); 이런것이 예이다.
또한 배열은 원소를 연속적으로 저장하는 걸 의미한다.
let plots = [20, 5, 8, 15, 20]; 이런식으로 사용이 된다.
또한 프로토타입이란 것이 존재하는데 그것은 객체의 모양을 가진 틀을 의미한다.
function Account(owner, code, balance 이런것들이 예이다.